# Flyers folder (for you — not shown on the website)

1. Drop images here.
2. Run: `npm run scan-flyers` (every image becomes **4∶5** or **9∶16** — whichever is closer).
3. On localhost, click **Reorder** to drag tiles, then **Save order** → replace `order.json` in this folder → run scan again.
